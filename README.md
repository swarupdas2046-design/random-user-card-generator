# Random User Card Generator 👤

A simple JavaScript project that fetches random users from an API and displays them as profile cards on the webpage.

## 🚀 Features
. Fetches random users using **Random User API**
. Dynamically creates user cards using **document.createElement**
. Displays user image, name, and city
. Refresh button to load new users without reloading the page
. Beginner-friendly DOM manipulation project

## 🛠️ Technologies Used
. HTML
. CSS
. JavaScript (DOM Manipulation)
. Fetch API


